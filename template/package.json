{
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "dev": "NODE_ENV=dev node --watch server.config.ts & node --watch-path=./app esbuild.config.ts",
    "start": "node esbuild.config.ts && node server.config.ts"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "ws": "^8.18.0"
  }
}