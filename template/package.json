{
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "dev": "NODE_ENV=dev node --watch server.config.ts",
    "start": "node server.config.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}